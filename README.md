# Setup

This project was built using Python 3.12.3.

Clone the repository.
```bash
git clone https://github.com/queue-model-enegep/queue_model_enegep_2026
```

## Ubuntu / Debian

Install Graphviz on your system.
```bash
sudo apt-get update
sudo apt-get install graphviz
```

Create and activate a virtual envrironment on the project directory.
```bash
cd queue_model_enegep_2026
python3 -m venv .venv
source .venv/bin/activate
```

Install Dependencies.
```bash
pip3 install -r requirements.txt
```

With the virtual environment activated, run the main script to get the plots and the model results.
```bash
python3 main.py
```

## MacOS

Install Graphviz on your system.
```bash
brew install graphviz
```

Create and activate a virtual envrironment on the project directory.
```bash
cd queue_model_enegep_2026
python3 -m venv .venv
source .venv/bin/activate
```

Install Dependencies.
```bash
pip3 install -r requirements.txt
```

With the virtual environment activated, run the main script to get the plots and the model results.
```bash
python3 main.py
```

## Windows

Install Graphviz on your system.
```bash
winget install graphviz
```

Clone the repository.
```bash
git clone <repository-url>
```

Create and activate a virtual envrironment on the project directory.
```bash
cd queue_model_enegep_2026
python -m venv .venv
.venv\Scripts\activate
```

Install Dependencies.
```bash
pip install -r requirements.txt
```

With the virtual environment activated, run the main script to get the plots and the model results.
```bash
python main.py
```
